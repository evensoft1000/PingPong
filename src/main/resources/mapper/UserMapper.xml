<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.evensoft.pingpong.mapper.UserMapper">
    <select id="selecUser" resultType="com.evensoft.pingpong.dto.UserDto">
        SELECT SEQ
             , NAME
          FROM USER
    </select>
</mapper>